/** @typedef {import('prettier').Config} PrettierConfig */

const config = {
  trailingComma: "es5",
  tabWidth: 4,
  semi: false,
  singleQuote: true,
  jsxSingleQuote: true,
};

export default config;
