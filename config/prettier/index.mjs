/** @typedef {import('prettier').Config} PrettierConfig */

const baseConfig = {
  trailingComma: "es5",
  tabWidth: 2,
  semi: false,
  singleQuote: true,
  jsxSingleQuote: true,
  printWidth: 120,
  arrowParens: "always",
};

export default baseConfig;
